name: Build_My_Trix_APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build APK
        uses: pavi2410/web-to-apk-action@main
        with:
          title: "Trix Scoreboard"
          url: "https://trix-kingdom-score-almlthb6776.replit.app/"
